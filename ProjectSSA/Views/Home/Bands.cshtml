@model IEnumerable<ProjectSSA.ViewModels.BandsVM>

@{
    ViewBag.Title = "Bands";
}

<hgroup class="title">
    <h1>@ViewBag.Title.</h1>
    <h2>@ViewBag.Message</h2>
</hgroup>

@*<article id="articleBand">
    <table>
        @foreach (var item in Model) 
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    <img src="@Url.Content("~/" + @item.Picture)" alt="IMAGES" width="100" height="100"/>
                </td>
                <td>
                    @Html.ActionLink("Details", "Details", new { id=item.ID })
                </td>
            </tr>
        }
    </table>
</article>*@

<article id="articleBand">
        @foreach (var item in Model) 
        {
            
            <div class="BandBlok">
                <div class="BandBlokName">@Html.ActionLink(item.Name, "Details", new { id=item.ID })</div>
                <div class="BandBlokImage"><img src="@Url.Content("~/" + @item.Picture)" alt="IMAGES" width="150" height="150"/></div>
            </div>

        }
</article>
